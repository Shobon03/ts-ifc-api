---
title: Frontend
description: Aplicação React com TanStack Router
---

## Visão Geral

Frontend construído com **React 19**, **TanStack Router** e **Tailwind CSS**.

**Porta dev**: 3001 (Vite dev server)
**Porta produção**: 3000 (servido pelo backend Node.js)

## Estrutura

```typescript
frontend/src/
├── main.tsx                // Entry point
├── routes/                 // File-based routing
│   ├── __root.tsx          // Root layout
│   ├── index.tsx           // Home/Landing page
│   ├── about.tsx           // Sobre o projeto
│   ├── model-generation.tsx    // Geração de IFC
│   ├── model-transformation.tsx // Conversão e transformação
│   └── model-validation.tsx     // Validação IFC
├── components/
│   ├── file-conversion-with-websocket.tsx
│   ├── conversion-progress.tsx
│   └── websocket-status.tsx
├── contexts/
│   └── WebSocketContext.tsx    // Global WebSocket Provider
├── hooks/
│   ├── useWebSocket.ts
│   └── useFileUpload.ts
└── styles/
    └── globals.css
```

## Tecnologias

```json
{
  "react": "19.1.1",
  "@tanstack/react-router": "1.132.7",
  "@tanstack/react-query": "5.90.2",
  "tailwindcss": "4.1.13",
  "vite": "7.1.7"
}
```

## Roteamento (TanStack Router)

```typescript
// routes/__root.tsx
import { createRootRoute, Outlet, Link } from '@tanstack/react-router'
import { WebSocketProvider } from '../contexts/WebSocketContext'

export const Route = createRootRoute({
  component: () => (
    <WebSocketProvider wsUrl='ws://localhost:3000/models/ws/conversion'>
      <div className="min-h-screen bg-gray-50">
        <nav className="bg-white shadow">
          <div className="max-w-7xl mx-auto px-4">
            <div className="flex justify-between h-16">
              <div className="flex space-x-8">
                <Link to="/">Início</Link>
                <Link to="/about">Sobre</Link>
                <Link to="/model-generation">Geração de IFC</Link>
                <Link to="/model-transformation">Transformação</Link>
                <Link to="/model-validation">Validação</Link>
              </div>
            </div>
          </div>
        </nav>

        <main className="max-w-7xl mx-auto py-6">
          <Outlet />
        </main>
      </div>
    </WebSocketProvider>
  )
})
```

```typescript
// routes/model-generation.tsx
import { createFileRoute } from '@tanstack/react-router'
import { FileConversionWithWebSocket } from '../components/file-conversion-with-websocket'

export const Route = createFileRoute('/model-generation')({
  component: ModelGenerationPage
})

function ModelGenerationPage() {
  return (
    <div className="space-y-6">
      <h1 className="text-3xl font-bold">Geração de IFC</h1>
      <p className="text-gray-600">Converta arquivos Revit (.rvt) ou Archicad (.pln) para IFC</p>
      <FileConversionWithWebSocket />
    </div>
  )
}
```

## Componentes

### FileUpload

```typescript
// components/file-upload.tsx
import { useState } from 'react'

export function FileUpload({ onUpload, accept = '.rvt,.pln,.ifc' }) {
  const [file, setFile] = useState<File | null>(null)
  const [uploading, setUploading] = useState(false)

  const handleChange = (e: React.ChangeEvent<HTMLInputElement>) => {
    if (e.target.files && e.target.files[0]) {
      setFile(e.target.files[0])
    }
  }

  const handleSubmit = async () => {
    if (!file) return

    setUploading(true)

    const formData = new FormData()
    formData.append('file', file)

    try {
      await onUpload(formData)
    } finally {
      setUploading(false)
    }
  }

  return (
    <div className="space-y-4">
      <div className="flex items-center justify-center w-full">
        <label className="flex flex-col items-center justify-center w-full h-64 border-2 border-gray-300 border-dashed rounded-lg cursor-pointer bg-gray-50 hover:bg-gray-100">
          <div className="flex flex-col items-center justify-center pt-5 pb-6">
            <svg className="w-8 h-8 mb-4 text-gray-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M7 16a4 4 0 01-.88-7.903A5 5 0 1115.9 6L16 6a5 5 0 011 9.9M15 13l-3-3m0 0l-3 3m3-3v12" />
            </svg>
            <p className="mb-2 text-sm text-gray-500">
              <span className="font-semibold">Click to upload</span> or drag and drop
            </p>
            <p className="text-xs text-gray-500">
              {accept.replace(/\./g, '').toUpperCase()} files
            </p>
          </div>
          <input type="file" className="hidden" accept={accept} onChange={handleChange} />
        </label>
      </div>

      {file && (
        <div className="text-sm text-gray-600">
          Selected: {file.name} ({(file.size / 1024 / 1024).toFixed(2)} MB)
        </div>
      )}

      <button
        onClick={handleSubmit}
        disabled={!file || uploading}
        className="w-full px-4 py-2 text-white bg-blue-600 rounded-md hover:bg-blue-700 disabled:bg-gray-400"
      >
        {uploading ? 'Uploading...' : 'Convert'}
      </button>
    </div>
  )
}
```

### ConversionProgress

```typescript
// components/conversion-progress.tsx
export function ConversionProgress({ progress, message, status }) {
  return (
    <div className="space-y-2">
      <div className="flex justify-between text-sm">
        <span>{message}</span>
        <span>{progress}%</span>
      </div>

      <div className="w-full bg-gray-200 rounded-full h-2.5">
        <div
          className={`h-2.5 rounded-full transition-all ${
            status === 'completed' ? 'bg-green-600' :
            status === 'error' ? 'bg-red-600' :
            'bg-blue-600'
          }`}
          style={{ width: `${progress}%` }}
        />
      </div>
    </div>
  )
}
```

## WebSocket Context Provider

### Global WebSocket State

O frontend usa um **WebSocketProvider** global para gerenciar uma única conexão WebSocket compartilhada por toda a aplicação.

```typescript
// contexts/WebSocketContext.tsx
import { createContext, useContext, useEffect, useRef, useState } from 'react'

interface Job {
  jobId: string
  status: 'pending' | 'processing' | 'completed' | 'error'
  progress: number
  message: string
  outputPath?: string
}

interface WebSocketContextValue {
  isConnected: boolean
  jobs: Map<string, Job>
  sendMessage: (message: any) => void
}

const WebSocketContext = createContext<WebSocketContextValue | null>(null)

export function WebSocketProvider({ 
  wsUrl, 
  children 
}: { 
  wsUrl: string
  children: React.ReactNode 
}) {
  const [isConnected, setIsConnected] = useState(false)
  const [jobs, setJobs] = useState<Map<string, Job>>(new Map())
  const ws = useRef<WebSocket | null>(null)

  useEffect(() => {
    const connect = () => {
      ws.current = new WebSocket(wsUrl)

      ws.current.onopen = () => {
        setIsConnected(true)
        console.log('WebSocket conectado')
      }

      ws.current.onclose = () => {
        setIsConnected(false)
        // Reconecta após 3 segundos
        setTimeout(connect, 3000)
      }

      ws.current.onmessage = (event) => {
        const data = JSON.parse(event.data)
        
        if (data.type === 'job_update') {
          setJobs(prev => new Map(prev).set(data.jobId, {
            jobId: data.jobId,
            status: data.status,
            progress: data.progress,
            message: data.message,
            outputPath: data.outputPath
          }))
        }
      }
    }

    connect()

    return () => {
      ws.current?.close()
    }
  }, [wsUrl])

  const sendMessage = (message: any) => {
    if (ws.current && isConnected) {
      ws.current.send(JSON.stringify(message))
    }
  }

  return (
    <WebSocketContext.Provider value={{ isConnected, jobs, sendMessage }}>
      {children}
    </WebSocketContext.Provider>
  )
}

export function useWebSocketContext() {
  const context = useContext(WebSocketContext)
  if (!context) {
    throw new Error('useWebSocketContext must be used within WebSocketProvider')
  }
  return context
}
```

### useFileUpload

```typescript
// hooks/useFileUpload.ts
import { useState } from 'react'

export function useFileUpload(endpoint: string) {
  const [uploading, setUploading] = useState(false)
  const [progress, setProgress] = useState(0)
  const [error, setError] = useState<string | null>(null)

  const upload = async (formData: FormData) => {
    setUploading(true)
    setError(null)

    try {
      const response = await fetch(endpoint, {
        method: 'POST',
        body: formData
      })

      if (!response.ok) {
        throw new Error(`Upload failed: ${response.statusText}`)
      }

      return await response.json()

    } catch (err) {
      setError(err.message)
      throw err

    } finally {
      setUploading(false)
    }
  }

  return { upload, uploading, progress, error }
}
```

## Exemplo Completo com WebSocketProvider

```typescript
// routes/model-generation.tsx
import { FileUpload } from '../components/file-upload'
import { ConversionProgress } from '../components/conversion-progress'
import { useWebSocketContext } from '../contexts/WebSocketContext'
import { useState, useEffect } from 'react'

export function ModelGenerationPage() {
  const [jobId, setJobId] = useState<string | null>(null)
  const { isConnected, jobs } = useWebSocketContext()

  // Obtém job atual do Map global
  const currentJob = jobId ? jobs.get(jobId) : null

  const handleUpload = async (formData: FormData) => {
    const response = await fetch('http://localhost:3000/models/generate-ifc', {
      method: 'POST',
      body: formData
    })

    const data = await response.json()
    setJobId(data.jobId)

    // WebSocket automaticamente recebe atualizações via Provider
  }

  return (
    <div className="max-w-2xl mx-auto space-y-6">
      <h1 className="text-3xl font-bold">Geração de IFC</h1>

      <div className="bg-white rounded-lg shadow p-6 space-y-4">
        {/* Indicador de status WebSocket */}
        <div className="flex items-center justify-between">
          <span className="text-sm font-medium">Status da Conexão:</span>
          <div className="flex items-center space-x-2">
            <div className={`w-2 h-2 rounded-full ${isConnected ? 'bg-green-500' : 'bg-red-500'}`} />
            <span className="text-sm">{isConnected ? '🟢 Conectado' : '🔴 Desconectado'}</span>
          </div>
        </div>

        <FileUpload
          onUpload={handleUpload}
          accept=".rvt,.pln"
        />

        {currentJob && (
          <ConversionProgress
            progress={currentJob.progress}
            message={currentJob.message}
            status={currentJob.status}
          />
        )}
      </div>
    </div>
  )
}
```

## Build e Deploy

```bash
# Desenvolvimento
npm run dev

# Build produção
npm run build

# Preview build
npm run preview
```

**Output**: `dist/` (SPA estático)

## Variáveis de Ambiente

```bash
# .env.development
VITE_API_URL=http://localhost:3000
VITE_WS_URL=ws://localhost:3000

# .env.production
VITE_API_URL=https://seu-dominio.com
VITE_WS_URL=wss://seu-dominio.com
```

```typescript
// Uso no código
const apiUrl = import.meta.env.VITE_API_URL
const wsUrl = import.meta.env.VITE_WS_URL

// Exemplo
fetch(`${apiUrl}/models/generate-ifc`, { ... })
```
